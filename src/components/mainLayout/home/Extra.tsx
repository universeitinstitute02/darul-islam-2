import React from 'react';

/* ── UI COMPONENTS ── */

const SectionHeading = ({ children }: { children: React.ReactNode }) => (
  <div className="text-center mb-6">
    <h2 className="text-2xl font-bold text-green-800 inline-block relative">
      {children}
      <span className="block h-0.5 bg-green-700 mt-1 mx-auto w-3/4 rounded-full" />
    </h2>
  </div>
);



/* ── ICONS ── */





/* ── MAIN PAGE ── */

export default function Extra() {
  return (
    <main className="min-h-screen bg-[#fafaf8]" style={{ fontFamily: "'Hind Siliguri', sans-serif" }}>

      

      

      

      

    </main>
  );
}